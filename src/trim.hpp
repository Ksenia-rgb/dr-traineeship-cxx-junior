#ifndef TRIM_HPP
#define TRIM_HPP

namespace stdsimd
{
  void TrimRight(char* str);
  void TrimRightOne(char* str);
}
namespace intrinsimd
{
  void TrimRight(char* str);
}
namespace easy
{
  void TrimRight(char* str);
}

#endif
